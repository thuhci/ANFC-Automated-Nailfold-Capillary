from setuptools import find_packages, setup

setup(
    name="nailfold_data_preprocess",
    version="0.1.0",
    description="nailfold raw data process",
    packages=find_packages(),
    install_requires=[
        
    ]
)

